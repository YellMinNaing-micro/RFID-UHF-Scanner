# Vendor native libraries (local only)

Place vendor-authorized native libraries under ABI-specific directories such as `arm64-v8a/` and `armeabi-v7a/`. All `.so` files below this directory are ignored by Git and must not be committed.
