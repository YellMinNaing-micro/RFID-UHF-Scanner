# Vendor JARs (local only)

Place vendor-authorized RFID SDK JAR files in this directory. JAR files and extracted vendor classes are ignored by Git and must not be committed.

The current bridge expects `uhfcom13_v15.jar` and `UHF67_v3.6.jar`. Confirm the correct files and redistribution terms with the device vendor.
